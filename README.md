# Video_Monney_Graber_DGHACK
a playwright bot to automatise video watch and analysing

# Command
```bash
    yarn install
     npx playwright test watch.spec.ts -g "watch\s+video$" --repeat-each=1000 --workers 20
```